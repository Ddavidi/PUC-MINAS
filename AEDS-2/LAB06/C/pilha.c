#include <stdio.h>

typedef struct Pilha {
    int max;
    int n;
    int *array;


} Pilha;

Pilha* newPilha(int max){
    Pilha* p = (Pilha*) malloc(sizeof(Pilha));
    
}

int main(){

    while(scanf != EOF){

    }

    return 0;
}